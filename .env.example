# # Copia este archivo a .env (no lo subas al repo) o exporta las variables
# manualmente antes de correr los scripts.

# Proyecto: Russali-ux's Project
SUPABASE_URL=https://ggbnfdaxtsngsjssrwrl.supabase.co
# Settings -> API -> service_role (NUNCA la anon/public key, y nunca la subas al repo)
SUPABASE_SERVICE_ROLE_KEY=

# No se necesita ninguna API key para embeddings: el paso 2 usa un modelo
# local de sentence-transformers (gratis, corre en tu máquina).
